[tool.ruff]
line-length = 100
target-version = "py311"
extend-exclude = [".venv", "mlruns", "mlartifacts"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["E501"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
